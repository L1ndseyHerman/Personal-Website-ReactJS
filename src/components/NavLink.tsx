import React from "react";

import classes from "./NavLink.module.css";

const NavLink: React.FC = () => {
  return <p className={classes.navLink}>Not a real link yet.</p>;
};

export default NavLink;
